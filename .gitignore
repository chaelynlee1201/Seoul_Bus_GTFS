# secrets
.env

# logs (API 키가 에러 URL에 노출될 수 있어 내용은 제외, 폴더는 유지 — SLURM --output 경로)
logs/*
!logs/.gitkeep

# large raw / intermediate
data/raw/
data/boundary/
data/raw_log_*.csv
*.geojson

# python
__pycache__/
*.pyc

# OS
.DS_Store

# 실수로 만들어진 중첩 클론 제외
Seoul_Bus_GTFS/
